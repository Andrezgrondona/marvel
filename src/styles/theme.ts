// src/styles/theme.ts
import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    primary: {
      500: '#3182ce', // Color primario
    },
  },
  // Puedes agregar más configuraciones aquí
});

export default theme;